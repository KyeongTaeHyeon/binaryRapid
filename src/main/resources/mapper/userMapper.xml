<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.binary.rapid.user.mapper.UserMapper">

    <!-- 회원가입 -->
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userId" keyColumn="user_id">
        INSERT INTO tb_user(id, password, nickname, name, taste, birth, email, social, gender, role)
        VALUES (
        #{id},
        #{password},
        #{nickName},
        #{name},
        #{taste, jdbcType=VARCHAR},
        #{birth, jdbcType=VARCHAR},
        #{email},
        #{social},
        #{gender, jdbcType=VARCHAR},
        #{role}
        );
    </insert>

    <select id="duplicateUserId" parameterType="userDto" resultType="int">
        SELECT * FROM tb_user WHERE id = #{id};
    </select>


</mapper>