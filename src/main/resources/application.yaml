spring:
  application:
    name: rapid
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    # com.mysql.cj.jdbc.Driver
    url: jdbc:log4jdbc:mysql://proxogus.codns.com:32270/rapid?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: rapid_user
    password: rapid!234
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 20MB
jwt:
  secret: ${SECRET_KEY}   # ← 환경변수에서 주입 (권장)
  access-token-expire-time: 1800000   # 30분 (ms)
  refresh-token-expire-time: 1209600000 # 14일 (ms)
  
mybatis:
  mapper-locations: classpath:mapper/*.xml
  config-location: classpath:mybatis-config.xml
